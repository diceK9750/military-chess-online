# 実装計画と判断事項

本書は実装順と受入条件を定める設計書。第1・第2段階は完了し、第3段階の対CPU戦と第4段階のローカル保存・暗号化ファイルを実装した。初回公開は対CPU戦のみを目標とし、オンライン対人戦・認証・DB・サーバー処理は第7段階以降の将来拡張とする。正本は[GAME_RULES.md](GAME_RULES.md)、全体方針は[ARCHITECTURE.md](ARCHITECTURE.md)、将来のオンライン保護要件は[SECURITY.md](SECURITY.md)。保存・処理・操作の詳細は[DATA_MODEL.md](DATA_MODEL.md)、[SERVER_LOGIC.md](SERVER_LOGIC.md)、[USER_FLOW.md](USER_FLOW.md)に分ける。

## 1. 共通の進め方

各段階の完了条件を満たしてから次へ進む。自動化できる規則・データ・権限・通信障害の確認を自動化し、人間は視認性・指の操作感・実機固有の問題に集中する。画面幅の自動検査を実スマートフォン検証の代わりとして報告しない。

全段階で、16種類23枚、各23配置地点、HQ各1論理地点・最大1駒、突破口B4↔B5/E4↔E5、既存勝敗表、終局順序を保持する。ルール変更はこの実装計画から行わない。実装中に未定義を発見したら、正式仕様の更新を承認してから進める。

将来のオンライン対人戦では、対局中の敵種類を通信で送らず、手番・種類・合法手・勝敗・使用列をサーバーで再検証する。client_move_id、state_version、ruleset_versionの既存契約をオンラインの全層で共有する。対CPUローカル対局では、CPU思考へ渡す情報をCPUの可視情報に限定する。ローカルの完全盤面は端末上に存在するため、オンラインの秘密保護を実証したものとは扱わない。

## 2. 第1段階：開発環境と最小画面

| 観点 | 内容 |
| --- | --- |
| 目的 | 外部サービス未接続で画面と検査の土台を確認できるようにする |
| 作るもの | 選定した開発環境、静的な対局一覧・盤面の最小画面、縦持ちレイアウト、文書リンク、自動テスト実行経路 |
| 作らないもの | 実認証、DB、オンライン対戦、本番配信設定、課金、秘密の実対局データ |
| 完了条件 | ローカルで起動しスマホ幅でも基本画面が横にはみ出さない。既存仕様へアクセス可能。自動チェックを1回の操作で実行可能 |
| 必要な自動テスト | 起動・表示のスモーク、複数画面幅のはみ出し、基本操作領域と文書リンク。架空の表示データだけを使用 |
| 人間の確認 | 縦持ち相当の表示で駒・文字・ボタンを識別できるか、PC表示も破綻しないかを短時間確認 |

I1はReact / TypeScript / Vite、Vitest / React Testing Library / Playwright、npmを採用済み。X1〜X5の承認済み部分を操作設計へ反映する。オンライン未実装の画面は準備中と明示する。X6等の未承認項目を確定しない。

## 3. 第2段階：純粋なゲーム規則の計算

| 観点 | 内容 |
| --- | --- |
| 目的 | ネットワークやDBに依存せず正式ルールを再現できるようにする |
| 作るもの | 論理盤面、駒構成、配置検証、合法手、突破口、HQ幾何、軍旗、勝敗表、飛行機・工兵、終局・PASS・50手の純粋な処理 |
| 作らないもの | 実対局の秘密管理、認証、通信、DB。秘密を持つ計算処理をブラウザの正本にする構成 |
| 完了条件 | 正式仕様の全規則に対応するテストが通る。同じ状態・手から同じ結果が得られる。P1/P2を交換して180度回転した結果が対応する |
| 必要な自動テスト | 下記の規則網羅表。ランダム生成状態も使用し位置一意・駒数保存・除去整合を確認 |
| 人間の確認 | 代表的な盤面図と文書の対応をレビュー。大量の手動対局で全規則を検証し直さない |

第2段階では実利用者の秘密データや公開APIを扱わない。計算テストは全種類が分かる架空の正本を入力してよい。対CPUローカル対局では端末内で完全盤面を処理するが、将来のオンライン対人戦で全盤面をブラウザへ配信する根拠にはしない。先手抽選は純粋な移動処理と分離し、対CPU戦では配置確定時にローカルで行う。オンライン対人戦では双方READY後にサーバーで一度だけ行う。

| 規則群 | 自動確認する境界 |
| --- | --- |
| 配置 | 16種類23枚、各23地点、欠落・重複・盤外・禁止入口・軍旗最後列、地雷HQ可 |
| 移動 | 通常1マス、前方2マスの敵味方妨害、工兵の直線と妨害、飛行機の縦長距離・横1・味方飛越・敵停止戦闘 |
| HQ | 各1地点、仮想アンカー非占有、双方向隣接、途中中継と列変更禁止、別手の別ポート退出 |
| HQ間飛行機 | C1〜C8/D1〜D8の各6中間地点、両方向、各位置に敵/味方、到着味方不可・敵戦闘、片列/両列の可否、占領資格なし |
| 突破口 | 通常駒はB4/B5・E4/E5のみ。工兵のC/D境界不可、飛行機は可 |
| 戦闘・軍旗 | 正式15×15表の全225組、有効駒種変換、後方なし・味方/敵HQ、同種相打ち、軍旗自身だけ除去・占領資格非継承 |
| 終局 | 占領→残存数→合法手→50手の順、双方0引き分け、片側0勝利、勝利と50手同時なら勝利 |
| PASS・手数 | 初手/着手後の片側なし、双方なし、手数とカウンター不変、49→50、戦闘で0、終局後PASSなし |

## 4. 第3段階：CPU対戦（初期実装）

| 観点 | 内容 |
| --- | --- |
| 目的 | 1人で正式規則による対CPU戦を始め、終局まで進められるようにする |
| 作るもの | かんたん・ふつうの選択、CPUから見える情報への変換、合法なCPU配置、先手抽選、CPU自動着手、対CPU画面と結果表示 |
| 作らないもの | 認証、招待、通信、DB、サーバー処理、実対局の再読込後復元 |
| 完了条件 | 利用者の配置確定からCPUの自動着手・終局まで進行し、CPUは合法手のみ選ぶ。人間とCPUで同じ規則処理を使う |
| 必要な自動テスト | 可視情報への変換、敵の秘密駒種変更に対する判断不変性、両難易度、合法配置・禁止地点、固定乱数種、P1/P2、手番・PASS・終局、画面操作 |
| 人間の確認 | スマートフォン相当の縦持ちとPCで配置・着手・結果の視認性を確認。実機確認は第6段階 |

CPU思考関数の入力は自軍の駒種・位置、敵の位置、公開履歴・戦闘結果、現在手番、合法手に限定する。相手の未公開駒種や秘密の初期配置を渡さない。かんたんは合法手から均等ランダムで選び、ふつうは公開情報に基づく評価値で選ぶ。評価値と重みは調整しやすい一か所に置く。固定乱数種で結果を再現できるようにする。CPU配置の戦略的な偏りは配置生成の選択であり、正式な配置制約の追加ではない。

CPU手番は人間の確定着手後、共通規則で盤面・終局を確定してから処理する。CPUは可視情報で選んだ手を共通の着手関数へ渡し、別の戦闘判定を持たない。CPU思考中は人間の二重操作を止める。対局本体はこの段階では再読込で失われる。

## 5. 第4段階：対CPU対局の自動保存・書き出し・読み込み

| 観点 | 内容 |
| --- | --- |
| 目的 | ブラウザを閉じたり再読込した後でもローカル対局を再開できるようにする |
| 作るもの | 準備中の配置と確定済み対局の自動保存・復元、パスワード付き`.mcsave`書き出し・読み込み、「続きから」、新規開始・読み込み時の置換確認 |
| 作らないもの | アカウント・クラウド同期・オンライン対局保存 |
| 完了条件 | 盤面、初期配置、全駒状態、手番、棋譜、非戦闘カウンター、状態版、ルール版、CPU難易度を矛盾なく復元できる。誤パスワードや改ざんで既存保存を破壊しない |
| 必要な自動テスト | 保存形式版・ルール版、破損・不正状態、暗号化往復・改ざん検出、終局状態、CPU情報境界、React画面とモバイル/PCブラウザ操作 |
| 人間の確認 | 再読込、ブラウザ再起動、ファイル受け渡し後の再開とパスワード入力の操作感を実機で確認（第6段階） |

保存形式版とruleset_versionを区別する。CPUの独立した推理状態は現在存在せず、乱数種と履歴を保存する。ブラウザ保存には完全盤面が平文で含まれ、端末所有者に対する対局秘密の保証とは扱わない。ファイルはWeb CryptoによるPBKDF2-SHA-256とAES-256-GCMで暗号化し、復号前メタデータから駒情報を除く。読み込みでは初期配置から全着手をルール再生して整合性を確認する。

## 6. 第5段階：スマートフォン操作と表示の完成

縦持ちで配置から終局まで横スクロールなしに操作でき、駒・ボタン・現在手番・直近の出来事を見分けられるよう改善する。第5段階では23枚の配置一覧と合法交換先、着手前確認、直前着手・戦闘・自動PASS、折りたたみ履歴、終局後の全駒情報、保存画面と遊び方を整える。320/360/390/430/768/1280pxのブラウザ表示とキーボード操作を自動確認し、実機の指操作と画面端の安全領域は第6段階で確認する。正式ルール、保存形式、暗号化、CPUの情報境界は変更しない。

## 7. 第6段階：GitHub Pages公開と実機確認

静的フロントエンドをGitHub Pagesへ公開し、公開URLでの動作と実スマートフォンでの配置・着手・保存・再開、Web Cryptoを使うファイル保存・読み込みを確認する。公開するのは対CPU戦のみ。GitHub Pages設定と実機確認はこの段階で行う。現段階のPlaywrightによるモバイル画面幅の検証を実機動作の証拠として扱わない。

## 将来のオンライン対人戦拡張（第7段階以降）

以下は承認済みのオンライン設計を保持するための計画であり、初回の対CPU公開の完了条件には含めない。認証・招待・サーバー保存・秘密情報分離・遠距離通信をこの順で整え、手番通知は任意の追加機能とする。

### 第7段階：保存とサーバー処理（旧第3段階）

| 観点 | 内容 |
| --- | --- |
| 目的 | 認証・永続化・原子的処理・情報秘匿を実証する |
| 作るもの | 認証連携、利用者、対局作成・招待、配置保存・READY/LOCK、着手API、再送記録、RLS、閲覧者別応答、旧版対応、バックアップ手順 |
| 作らないもの | 本番公開、豪華な画面・演出、任意のパス・投了、通知、成績、第三者観戦 |
| 完了条件 | 1手の状態・棋譜・再送記録が原子的に保存される。参加者以外は読めず、対局中の敵種類が一切応答にない。成功再送が二重実行されない |
| 必要な自動テスト | 認証・参加権限、RLS/直接DB/RPC、敵種類を入れ替えても公開応答が不当に変わらない検査、READY解除対開始の競合、招待枠競合、並列着手、異列再送、古い版、途中失敗・応答消失、復旧整合 |
| 人間の確認 | 承認済みログイン・招待の実メール動線を代表ケースで確認。サーバーの秘密管理をレビュー |

オンライン拡張のこの段階で初めて承認された外部開発環境を接続する。先にI2のトランザクション実行方式と、参加待ち中の配置開始時期を決める。X1〜X5の承認済み方針を維持する。ローカル対CPU戦の完全盤面をオンライン対人戦のクライアントへ流用しない。ロックから確定まで同じDBトランザクションで処理できることを先に小さく検証する。開始抽選は一度だけ、成功した開始の再実行で変わらないことも確認する。

データの旧版移行テストでは、ruleset_versionを固定した既存対局・保存棋譜を継続して解釈できることを検査する。日時を進める試験で手番期限・自動削除が発生しないことを確認する。

### 第8段階：実際の2人対戦（旧第4段階）

| 観点 | 内容 |
| --- | --- |
| 目的 | 独立した2利用者の完全非同期対戦を成立させる |
| 作るもの | 最小画面と保護済みサーバーを接続し、別アカウントの招待参加・交互着手・履歴・終局を通す |
| 作らないもの | 常時接続前提の設計、演出の作り込み、通知必須の進行 |
| 完了条件 | 別ブラウザまたは端末で配置から終局まで可能。相手が閉じても進められ、再認証・別端末で最新状態に戻れる |
| 必要な自動テスト | 2つの独立セッションのE2E、別アカウントキャッシュ混在防止、オフライン・再接続・二重タップ・再読込、保留ID消失からの復元、終局公開の参加者限定 |
| 人間の確認 | 2アカウントで代表対局を短く行い、片方を閉じて再開。相手種類が見えないことと操作の流れを確認 |

数か月を実際に待つことをゲートにしない。保存済み対局を残したままセッション切れ・日時進行・プロセス再起動を自動化して長期復帰を検査する。ただしこれを実際の数か月運用実績と呼ばない。

### 第9段階：オンライン対人戦のスマートフォン操作改善（旧第5段階）

| 観点 | 内容 |
| --- | --- |
| 目的 | 誤操作を減らし、長期中断後も状況を把握しやすくする |
| 作るもの | 駒選択、移動先表示、C/D経路選択、承認済みの最終確認、控えめな戦闘結果表示、前回着手・PASS・履歴、再開案内 |
| 作らないもの | 相手種類の勝敗予測、過剰な演出、取消ルールの追加、全機能の新規画面化 |
| 完了条件 | 縦持ちで基本操作が完結。自分の番・前回結果が最初に分かる。通信確認中に二重送信を誘発しない |
| 必要な自動テスト | 複数幅・文字サイズ、横はみ出し、タップ領域・フォーカス、P2回転時のC/D対応、送信状態遷移、秘密のDOM/ラベル/画像参照への混入 |
| 人間の確認 | 実際の指で少数の代表操作を確認。文字と駒の判別、経路表示、再開時の理解、演出を減らした表示を確認 |

第1段階から縦持ち対応は必須で、対CPU戦では第5段階までに完成させる。オンライン対人戦では通信時の表示と操作を追加で確認する。

### 第10段階：オンライン対人戦の公開と実機確認（旧第6段階）

| 観点 | 内容 |
| --- | --- |
| 目的 | 選定した公開環境と実スマートフォンで安全に利用できることを確認する |
| 作るもの | 第一候補GitHub Pagesの静的公開、承認したSupabase環境との接続、認証リダイレクト、運用・復旧手順 |
| 作らないもの | サーバー秘密鍵のフロント埋め込み、検証省略の本番投入、初版必須でない追加機能 |
| 完了条件 | 公開先でも認証・招待・着手・再開・終局が動作。秘密鍵不在、権限、バックアップ復旧を確認。実スマートフォン2台・別ネットワークで確認済み |
| 必要な自動テスト | ビルド成果物・Gitの秘密検査、公開URLのスモーク、認可回帰、TLS接続・リダイレクト設定、障害復旧、API応答と表示情報の漏洩検査 |
| 人間の確認 | 実機2台でメールリンク→参加→配置→着手→バックグラウンド復帰を代表経路として確認。別回線で非同期利用を確認 |

第7段階はオンライン開発環境、第10段階はオンライン公開環境の確認。実サービス作成や公開は各工程で行う。

### 第11段階：オンライン対人戦の任意の追加機能（旧第7段階）

| 観点 | 内容 |
| --- | --- |
| 目的 | 初版完成後に必要性のある機能だけを追加する |
| 作るもの | 個別に承認された手番通知、自分用推理メモ、完全な対局再生、成績、陣形保存、プロフィール編集、効果音・視覚演出 |
| 作らないもの | 全候補の一括実装、ルール承認なしの勝敗変更、敵秘密の開示範囲拡大 |
| 完了条件 | 各機能を切り離して受入可能。無効でも既存対局を継続でき、旧対局・旧版と秘匿を壊さない |
| 必要な自動テスト | 通知重複と秘密なし、本人メモの認可、再生のC/D・版一致、陣形の現行配置検証、プロフィールの権限、既存回帰 |
| 人間の確認 | 承認された機能だけを代表ケースで確認。通知頻度や演出の負担を評価 |

オンライン対人戦では全駒・初期配置・完全棋譜・戦闘内容の終局後閲覧を必須とし、第11段階へ先送りしない。ここで任意なのは再生UIやアニメーション等の拡張。表示名項目を保存できる設計とプロフィール編集機能も区別する。

## 8. 未確定事項の分類

### 1. 実装上だけの選択

| ID | 推奨案 | 決定時期 |
| --- | --- | --- |
| I1（決定済み） | React / TypeScript / Vite、Vitest / React Testing Library / Playwright。npmとlockfile。純粋なルール層と表示・開発用保存を分離 | 第1・第2段階で採用 |
| I2 | 対局ロックと全更新を1DBトランザクションで完結させる。DB関数内実行かトランザクションを保持できるサーバー実行かを比較し、同じルール試験を満たす方式を選ぶ | オンライン第7段階前 |
| I3 | UUID等の不透明ID、UTC、MOVEとシステムイベントの分離、成功再送記録、作成用重複防止情報を採用する案。物理スキーマ・API形式・C/D項目名は次工程で確定 | オンライン第7段階前 |
| I4 | バックアップと復旧で正本・棋譜・版・再送記録をそろえる。頻度・保管先・復旧目標を運用要件と費用から選ぶ | オンライン第7〜10段階 |

これらは合理的な実装推奨であり、ゲームルールの追加承認を要する選択とは分ける。I2は複数のHTTP書き込みを並べるだけでは要件を満たさない。

### 2. 利用者体験の承認状況

| ID | 選択と推奨 | 代替・影響 | 承認が必要な時点 |
| --- | --- | --- | --- |
| X1（承認済み） | 電子メールMagic Linkを正式ログインの第一方式とする | 再送等の具体表示は後工程。今回は認証未実装 | 再承認不要 |
| X2（一部未決定） | 作成者=P1、参加者=P2は承認済み。先手は双方READY後のサーバー抽選 | 参加待ち中の配置可否だけ未決定 | オンライン配置フロー前に残項目を決定 |
| X3（主要方針承認済み） | 固定期限なし、認証必須、参加後再利用不可、参加前に作成者が無効化可、再発行時は旧招待失効 | 参加確認や共有の画面詳細は後工程。今回は実招待なし | 原則の再承認不要 |
| X4（主要方針承認済み） | 配置自動保存と「この配置で確定」。駒→合法移動先→確認→確定。HQ間両列合法は選択、片列は自動選択可 | オンライン保存・通信障害表示の詳細は後工程 | 原則の再承認不要 |
| X5（承認済み） | 表示名は任意。未設定は「あなた」「対戦相手」。メールを相手に表示しない | プロフィール編集は後工程 | 再承認不要 |
| X6 | オンライン対人戦の初版は時間による対局自動削除なし、終局済みも保持。放棄・削除・退会UIは提供しない案 | 提供する場合は共有棋譜の扱い等を承認する。放棄を勝敗へ変換しない | 該当機能または運用開始前 |

未決定の残項目を実装済みの仕様として扱わない。承認済み方針と将来の具体的な画面設計を区別する。

### 3. 勝敗やゲームルールに影響する選択

今回の設計で新たに発見した未解決事項はなし。U1〜U5、R1、飛行機のHQ間直接移動、既存勝敗表をそのまま使用する。投了・合意引き分け・放棄終局等は対CPU初回公開版へ追加しない。

## 9. 8文書の横断確認と受入の記録

| 確認軸 | 正本と設計上の対応 |
| --- | --- |
| 駒数・地点 | GAME_RULESとDATA_MODELで各16種類23枚・23配置地点。HQは各1地点 |
| 座標・突破口・飛行機 | GAME_RULESの正規座標、2突破口、HQ間C/D・各6中間地点をSERVER_LOGICで検証 |
| 秘匿・終局公開 | 対CPU戦はCPU思考の入力を可視情報へ限定。将来のオンライン対人戦ではSECURITYを全応答へ適用し、対局中は相手種類を送らず、終局後のみ参加者へ全棋譜等を公開 |
| 再送・状態版 | ARCHITECTUREのclient_move_idとstate_versionをDATA_MODELとSERVER_LOGICへ具体化。使用列も同一内容照合 |
| ルール版 | 作成時ruleset_version固定。保存・再生・復旧も旧版を維持 |
| PASS・50手 | プレイヤーのMOVEのみ手数増加、AUTO_PASSはイベントのみ。50手は非戦闘着手連続数で現実時間不使用 |
| 長期対局 | 対CPU戦はローカル保存・再開と暗号化ファイルでの手動移行を実装。オンライン対人戦はDBを正本とし、再認証・別端末・数か月不在に対応。放置による自動終局・削除なし |
| 対称性 | P1/P2の権限・表示情報は同じ方式。作成・参加の役割差は先手や勝敗の優遇にならない |
| 判断事項 | I1は採用済み。X1〜X5は将来のオンライン対人戦向け主要方針として承認済み。I2〜I4、参加待ち中配置、X6等は未決定。ゲームルールの再設計なし |

第1・第2段階では型検査、Vitest（正式戦闘表225組、移動・HQ・軍旗・終局・対称性・生成対局）、React Testing Library、Playwright、ビルドを実行する。Playwrightはモバイル・PCの配置、確認着手、C/D選択、終局、配置再読込と横はみ出しを検査する。実機の指操作・本番の秘密保護・サーバー競合は未検証。実行コマンドはREADMEに記載する。

2026-09-21の第1・第2段階確認：`npm run check`成功。型検査、Vitest 571件（ルール565件・画面部品6件）、Playwright 6件、Vite本番ビルドが通過。合計577件、失敗0件。Node 24.19.0 / npm 12.0.2、Chromiumを使用。320/390/768/1280pxの横はみ出しと、モバイル画面画像を確認。実スマートフォンでの動作・指操作は未実施。

2026-09-24の第3段階確認：`npm ci`と`npm run check`成功。型検査、Vitest 591件（既存571件、CPU18件、画面部品追加2件）、Playwright 10件（既存6件、対CPUのモバイル・PC各難易度4件）、Vite本番ビルドが通過。合計601件、失敗0件。対CPU画面の320px幅画像と横はみ出しを確認。対局の再読込後復元、実スマートフォン、GitHub Pages公開、オンライン秘密保護は未検証。

2026-09-25の第4段階確認：`npm ci`と`npm run check`成功。型検査、Vitest 617件（既存591件、保存・暗号化21件、画面追加5件）、Playwright 18件（既存10件、準備中の復元・対局自動保存・ファイル往復・破損対応をモバイル/PCで8件）、本番ビルドが通過。合計635件、失敗0件。320px幅のパスワード画面画像と横はみ出しを確認。パスワード不一致・暗号文改ざん・不正局面では現在の保存を維持する。実スマートフォン操作、ブラウザ再起動をまたぐ実運用、GitHub Pages公開、オンライン秘密保護は未検証。

2026-09-25の第5段階確認：`npm ci`と`npm run check`成功。型検査、Vitest 625件（既存617件、画面追加8件）、Playwright 20件（既存18件、スマートフォン操作と6画面幅のモバイル/PC追加2件）、本番ビルドが通過。合計645件、失敗0件。320/360/390/430/768/1280pxの横はみ出し、盤面の最小幅、キーボード操作、配置からCPU着手・保存・再開をChromiumで確認。実スマートフォンの指操作・安全領域とGitHub Pages公開は第6段階で確認する。ブラウザ内の完全盤面はオンライン対人戦の秘密保護を証明しない。
